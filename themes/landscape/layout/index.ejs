<section class="home-page">
  <section class="home-hero" aria-labelledby="home-hero-title">
    <p class="home-hero-kicker">Hi, I'm Casper.</p>
    <h1 id="home-hero-title" class="home-hero-title">Cybersecurity Student</h1>
    <div class="home-hero-roles">
      <span>Web Developer</span>
      <span>Robotics Builder</span>
    </div>
    <p class="home-hero-mantra">Learning. Building. Sharing.</p>
  </section>

  <h2 class="section-title">Recent Posts</h2>

  <% if (page.posts && page.posts.length) { %>
    <div class="writing-list">
      <% page.posts.each(function(post) { %>
        <article class="writing-item">
          <div class="writing-date"><%= date(post.date, 'YYYY-MM-DD') %></div>
          <div class="writing-title">
            <a href="<%= url_for(post.path) %>"><%= post.title %></a>
          </div>
        </article>
      <% }) %>
    </div>
  <% } else { %>
    <p>No posts yet.</p>
  <% } %>
</section>
